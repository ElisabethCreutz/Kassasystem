using System;
using System.Collections.Generic;
using System.Text;

namespace Kassasystem1.PreviousOrTesting
{
    internal class ProductIdComparison //: IComparer<Product>
    {
        //public int Compare(Product? x, Product? y)
        //{
        //    if (Object.ReferenceEquals(x, y))
        //        return 0;
        //    if (x is null)
        //        return 1;
        //    if (y is null)
        //        return -1;
        //    int result = IntCompare(x.ProductId, y.ProductId);
        //    if (result == 0)
        //        result = StringCompare(x.FirstName, y.FirstName);
        //    return result;
        //}
        //private static int IntCompare(int? x, int? y)
        //{
        //    if (Object.ReferenceEquals(x, y))
        //        return 0;
        //    if (x is null)
        //        return 1;
        //    if (y is null)
        //        return -1;
        //    return x.CompareTo(y);
        //}
    }
}
